# Runtime Environment

This Claude Code instance is running inside a Docker container.

**Container facts:**
- Working directory root: `/root`
- Workspace is mounted at `/root/workspace`
- Go, Node.js, Python3 are all available
- Shell is `/bin/bash`

